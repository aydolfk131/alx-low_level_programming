#include "main.h"

/**
 * _isdigit -A function that checkfor a digit
 * @c: variable to be checked
 * Return: 1 if c is a digit 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
